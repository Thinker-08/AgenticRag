# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
build/
dist/
.eggs/

# Virtualenvs
.venv/
venv/
env/

# Tooling caches
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Runtime data volumes, uploads, indexes, model weights — but keep the tracked fixtures.
data/*
!data/golden/
!data/sample.txt
models/
*.gguf
*.bin

# Local env + secrets (never commit real keys)
.env
.env.local
.env.*.local

# Editor / OS
.idea/
.vscode/
.DS_Store
*.swp

# Design docs (not published)
v2/
